# OAuth Scaffold (Disabled by Default)

This folder is reserved for a future standalone OAuth flow if the app is ever used outside embedded Whop contexts.

Current production auth uses Whop embedded token headers server-side (`lib/whop/auth.ts`).
